# API

This document describes the expected CLI and programmatic interfaces.

- `benchmark.py` is the main CLI entrypoint for performance comparisons.
- `pipeline.py` exposes `run_pipeline(prompt: str) -> dict`.
