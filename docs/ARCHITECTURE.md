# Architecture

This document describes the adapter, routing, cache, and verification architecture for RouteZero.

- `agents/` contains model routing and inference agent stubs.
- `pipeline/` holds orchestration state and graph definitions.
- `cache/` is intended for ChromaDB caching components.
- `router/` contains route controller and policy logic.
